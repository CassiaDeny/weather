import React, { Component } from 'react';
import SeaarchBar from '../containers/search-bar';
import SearchBar from '../containers/search-bar';

export default class App extends Component {
  render() {
    return (
      <div><SearchBar /></div>
    );
  }
}
